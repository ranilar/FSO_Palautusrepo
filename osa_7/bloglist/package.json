{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "bloglist-app: a Fullstack course exercise",
  "main": "index.js",
  "scripts": {
    "test": "test",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client && rm -rf server/build/* && mkdir -p server/build && cp -r client/dist/* server/build",
    "start": "npm run build && NODE_ENV=production npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "react-query": "^3.39.3"
  }
}
